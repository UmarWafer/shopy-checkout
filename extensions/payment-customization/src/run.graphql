query RunInput {
  cart {
    buyerIdentity {
      customer {
        id
        metafield(namespace: "checkout", key: "shipping") {
      value
    }
        }
    }
  }
  paymentMethods {
    id
    name
  }
}