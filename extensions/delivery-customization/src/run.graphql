query RunInput {
  cart {
    buyerIdentity {
      customer {
        id
        metafield(namespace: "checkout", key: "shipping") {
      value
    }
        }
    }
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
      }
    }
  }
}
